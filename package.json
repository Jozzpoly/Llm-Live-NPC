{
  "name": "llm-live-npc",
  "version": "0.0.0-p0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tsconfig.json --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.129.0"
  }
}
