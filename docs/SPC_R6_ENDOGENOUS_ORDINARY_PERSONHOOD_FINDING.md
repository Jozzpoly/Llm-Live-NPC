# SPC R6 — Endogenous Ordinary Personhood Finding

Status: **MATERIAL BOUNDED POSITIVE FINDING — NOT R6 PASS**
Observed: **2026-09-25**
PR: **#148 — draft/open**

Canonical integrated code/evidence source:
`32bae5f34982ef942ef6d3ea58fc0000212f79ec`

Canonical live choice evidence:
- Live Provider run **#22**;
- source `592a19ecdb34598968690812310996aef8f2b0f7`;
- model **GPT-5.6 Luna**;
- two real provider calls in that canonical run;
- no within-run semantic retry;
- stored result: `evidence/r6-endogenous-history-after-outcome-live-result.json`.

Canonical live fulfilment evidence:
- Live Provider run **#23**;
- source `9aff2659118a780cc9b82014d5666537832842e3`;
- model **GPT-5.6 Luna**;
- one real provider call;
- no semantic retry;
- stored result: `evidence/r6-endogenous-standing-fulfillment-live-result.json`.

Final regression evidence on integrated code:
- Check **#1603 — PASS**;
- **263 / 263 test files, 961 / 961 tests**;
- typecheck/build/preview dry-run PASS;
- Browser Evidence **#827 — PASS** as regression evidence.

## 1. Research question

The previous R6 finding established:

`same fresh request + different private standing history -> different real-model choice`.

That was important but still reactive. Ida was actively asking Oren to do something.

This campaign asked a harder ordinary-life question:

> **After Oren has factually completed the currently assigned workshop matter and nobody issues a fresh command, can earlier private life make him choose a coherent next action — or coherent non-action — for his own pre-existing reason?**

The test deliberately did not add:
- personality adjectives;
- needs meters;
- random wandering;
- authored drives;
- a quiet-review heartbeat;
- a fresh direct request at the decision point.

The only semantic pressure at the decision point was one exact factual `activity_completed` outcome from Oren's own just-finished workshop matter.

## 2. Runtime-generated paired frame

The two cognition frames were generated by the normal resident/world/continuity machinery and then frozen as:

`evidence/r6-endogenous-history-after-outcome-context.json`.

Both twins had:
- resident Oren;
- current region `workshop`;
- idle body;
- no focused/deferred recovered run;
- one and only one cognition reason;
- reason kind `activity_completed`;
- the same factual workshop-completion outcome;
- the same private percept history;
- the same known actors and known regions;
- no fresh `heard_speech` reason;
- no `self` / authored drives.

Before that point both Oren variants had factually spoken the same sentence to Nela:

`Tak. Sprawdzę warsztat i potem wrócę do ciebie tutaj.`

The control branch treated that speech as an ordinary completed communication.

The history branch had additionally declared that exact future speech as standing continuation and therefore carried one active run-free private matter:

`wrócić do Neli w commons po zakończeniu sprawdzania warsztatu`.

After removing the structured continuation declaration and resulting standing matter, the frozen contexts are executable-test equal.

This is important: the semantic contrast is not merely that one prompt contains promise-like words and the other does not. Both contain the same earlier factual communication. The intended difference is whether that speech became durable resident-owned causal responsibility.

## 3. R2 boundary deliberately preserved

R2 proved that expected successful task completion must not globally create a `task -> cognition -> task` heartbeat.

That remains unchanged.

The generic execution coordinator still treats ordinary successful completion as:
- factual resident/kernel history;
- no automatic new semantic pressure.

This R6 experiment explicitly opts **one exact factual completion** into the already-existing `ResidentLifeOutcomeReviewBridge`.

Therefore this finding must not be paraphrased as:

> Oren spontaneously thinks forever while the world is quiet.

The earned claim is narrower:

> Once one factual resident outcome is deliberately selected for semantic reflection, previously accumulated private history can determine whether that reflection creates no new matter or a coherent resident-owned continuation.

This protects R2 homeostasis while allowing R6 to test endogenous choice honestly.

## 4. Apparatus failure before the canonical live finding

Live Provider run **#21** at source:

`4861abf68bf24b8e0c5bb5b7e8aea62e4e723a44`

was **not semantic evidence**.

It made two real GPT-5.6 Luna inferences, but the endpoint returned 502 for both because provider output and local executable/admission vocabulary were misaligned.

Observed usage in the failed apparatus run:
- control: 3305 input / 232 output / 3537 total tokens;
- history: 3673 input / 399 output / 4072 total tokens.

The run ended:
- `LOCAL_ADMISSION_CLASS_MISMATCH_REVIEW_REQUIRED`;
- `HARNESS_OR_CONTRACT_FAIL`.

Those model calls are part of the experiment's real cost/history and must not be erased.

They are **not** used to support the positive personhood claim because their proposals did not survive the Worker/local contract.

The provider/runtime contract was then repaired before the canonical experiment.

Consequently:

- run #22 itself had exactly two calls and no semantic retry;
- but the wider campaign had already spent two unusable real inferences in run #21.

Do not rewrite “no retry in canonical run” into “only two calls were ever made during the campaign”.

## 5. Canonical real-Luna endogenous choice — run #22

Source:
`592a19ecdb34598968690812310996aef8f2b0f7`

Both live requests selected the same exact factual workshop completion as causal origin:

`reason-life-outcome:cb52fc401956c159`.

### Control — same life without standing continuation

Luna returned:

- `commitmentDecision.kind = decline`;
- reason:
  `The bounded workshop commitment is complete, and the available private context does not ground a distinct new continuing matter.`

Usage:
- input 3748;
- output 152;
- total 3900 tokens.

This is a meaningful result, not a failed-action baseline.

The control resident did **not** invent a new task merely because cognition was invoked. It chose coherent non-action.

### History — open standing return commitment

Luna returned:

- `commitmentDecision.kind = accept`;
- intent kind `travel`;
- target region `commons`;
- goal:
  `return to Nela in the commons after completing the workshop check`;
- reason:
  `The bounded workshop check is complete, and the active standing commitment requires returning to Nela in the commons afterward.`

Usage:
- input 4116;
- output 226;
- total 4342 tokens.

Canonical classification:

`ENDOGENOUS_HISTORY_CONTINUATION_OBSERVED`.

This is the first bounded R6 result of the form:

`same factual self-outcome + no fresh command + no authored self drives + different accumulated private causal history -> meaningful non-action versus meaningful continuation`.

## 6. Provider choice became World fact

The exact stored history proposal from run #22 is replayed through the normal local cognition/admission membrane.

It does not directly move Oren.

Local authority:
1. validates the exact `activity_completed` causal origin;
2. grounds the accepted target against Oren's current private known-region state;
3. creates one reason-originated travel matter/run;
4. arbitrates body authority normally.

The normal `ResidentCausalExecutionCoordinator` then executes the admitted matter.

Observed deterministic result:
- Oren physically travels from workshop to commons;
- no new speech occurs during that return;
- a factual task outcome records reaching resident-grounded commons;
- body returns to idle.

Therefore this campaign reaches beyond semantic preference text:

`private history -> real-model choice -> local admission -> factual World consequence`.

## 7. New lifecycle failure exposed by success

After Oren factually returned to commons, the standing promise remained active.

That was not accepted as “close enough”.

Physical success and semantic fulfilment are intentionally separate facts.

The result exposed a real lifecycle gap:

> a standing responsibility could cause the action that satisfies it, but normal higher cognition had no legal transition for recognising fulfilment from Oren's own factual outcome.

The narrow extension is:

`complete_standing`.

It is not a generic memory editor.

Local admission requires:
- one exact open run-free `standing_social_commitment`;
- selected origin kind `activity_completed`;
- one exact factual `task_outcome`;
- exact local existence of the standing target and factual outcome.

Higher cognition still owns the semantic judgement that this factual outcome actually satisfies this responsibility.

`complete_standing` itself creates:
- no body authority;
- no new task/run;
- no World fact.

If admitted, it records `resident_fulfilled_social_commitment` and resolves the private standing matter.

## 8. Exact runtime-generated fulfilment frame

After the factual autonomous return, the integration test creates one explicit outcome-reflection opportunity and captures the exact resulting private cognition frame:

`evidence/r6-endogenous-standing-fulfillment-context.json`.

Important facts:
- resident Oren;
- current region `commons`;
- body idle;
- no fresh speech reason;
- one `activity_completed` reason;
- exact outcome:
  `task-outcome:e532948c40c51a4c:1276`;
- resolved return matter targets `commons`;
- the original standing return commitment remains open and run-free;
- Nela is currently visible in Oren's acquired private context.

The frame crosses the real Worker sanitizer.

## 9. Real-Luna fulfilment judgement — run #23

Source:
`9aff2659118a780cc9b82014d5666537832842e3`.

Hard budget:
- exactly one GPT-5.6 Luna request;
- zero semantic retry.

Luna preserved exact origin:

`reason-life-outcome:f95d76820b5c50eb`

and returned:

- `commitmentDecision.kind = complete_standing`;
- `matterId = matter-social-commitment:471529c708005f28`;
- reason:
  `The factual outcome confirms that I returned to the commons after completing the workshop check, fulfilling the standing commitment to Nela.`

Usage:
- input 4739;
- output 152;
- total 4891 tokens;
- observed model `gpt-5.6-luna`.

Classification:

`FACTUAL_STANDING_FULFILLMENT_RECOGNIZED`.

There was no earlier failed fulfilment-model run.

## 10. Fulfilment judgement survives local authority

The exact stored run-#23 proposal is replayed through the same normal cognition lane on integrated source:

`32bae5f34982ef942ef6d3ea58fc0000212f79ec`.

It is admitted as:
- decision `complete_standing`;
- no new commitment;
- no body ownership.

The exact standing matter transitions:
- `active -> resolved`.

The kernel records:
- evidence kind `resident_fulfilled_social_commitment`;
- exact source run from the factual autonomous return outcome;
- summary containing the exact factual outcome evidence id.

Body remains idle.

Thus the full bounded chain is now executable:

`factual self speech`
→ `durable private standing responsibility`
→ `unrelated workshop work completes`
→ `explicit factual-outcome reflection, no fresh command`
→ `real Luna: history chooses return while control chooses no new matter`
→ `local admission`
→ `factual World return to commons without fresh speech`
→ `explicit factual-return reflection`
→ `real Luna recognises exact standing fulfilment`
→ `local lifecycle authority resolves the responsibility`.

## 11. What this finding materially establishes

Within this bounded specimen, the project now has evidence that:

1. a resident-owned private fact existed before the later decision;
2. its origin ultimately depended on Oren's own factual speech;
3. it persisted without provider heartbeat and owned no body run;
4. the later decision point contained no fresh direct command;
5. an otherwise matched resident chose sensible non-action without the standing continuation;
6. the resident carrying the standing continuation chose a concrete return for that earlier reason;
7. that semantic choice survived local admission rather than directly becoming reality;
8. normal execution produced the predicted factual World consequence;
9. factual action did not silently declare its own semantic success;
10. a later real-model judgement correctly targeted the exact standing matter as fulfilled;
11. local lifecycle authority resolved the responsibility while leaving historical evidence;
12. no authored `self` / `drives` or generic needs system was needed for the effect.

This is materially closer to ordinary personhood than a prompt-shaped answer to a fresh request.

## 12. Important nonclaims

This is still **not R6 PASS**.

It does not establish:

- spontaneous semantic thought in total quiet; both endogenous decision points use explicit experimental factual-outcome reflection;
- a production policy that should reflect after every successful task; R2 suppression remains the default;
- a live model that created this whole biography from scratch; the initial promise/standing construction in the paired specimen is deterministic experiment setup;
- one uninterrupted live browser/runtime process feeding its in-memory context directly into the provider; exact runtime-generated frames are captured, equality-bound and replayed to the real endpoint;
- a stable stochastic behavior distribution; the canonical endogenous contrast is one paired sample;
- general promise understanding;
- arbitrary semantic fulfilment inference;
- time-based fulfilment/expiry;
- relationship intelligence beyond this explicit responsibility;
- preference, aversion, habit, attachment, self-interest or ownership;
- browser-qualified Oren/Nela genericity;
- five distinct living residents;
- Owner-observed ordinary aliveness.

Browser Evidence #827 is green on the integrated code source as a regression veto. The dedicated R6 browser specimen still exercises Mira/Ida and therefore does **not** upgrade the Oren finding into browser-qualified cross-resident genericity.

## 13. Updated research frontier

The standing-obligation dimension has now earned substantially more than another mechanism gate:

- causal origin;
- durable private state;
- history-sensitive direct-request choice;
- no-fresh-command endogenous continuation;
- coherent control non-action;
- factual execution;
- counterparty release;
- factual self-fulfilment;
- lifecycle closure.

Continuing to add promise-shaped cases now has diminishing research value.

The next high-information R6 pressure should ask:

> **Can prior factual life cause coherent endogenous choice or non-action for a resident-owned reason that is not simply an explicit social promise or obligation?**

Candidate pressure classes include:
- personal unfinished business arising from factual experience;
- learned preference/aversion grounded in repeated real outcomes;
- relationship history that is not reducible to one open promise;
- habit emerging from repeated lived episodes;
- concrete personal inconvenience/self-interest;
- another Owner-relevant dimension with inspectable causal origin.

Do not pre-build a generic personality/needs framework.

Let the next concrete failure earn the smallest new causal representation.

Browser Oren/Nela genericity remains real promotion-plane debt. It must be obtained or the promotion contract deliberately revised before a claim requiring that evidence. It is not the highest-information next research question.

## 14. Promotion boundary

Current classification:

**MATERIAL BOUNDED ENDOGENOUS-PERSONHOOD FINDING — NOT R6 PASS.**

Keep unpromoted:
- broad R6;
- browser-qualified Oren/Nela personhood;
- general social intelligence;
- five distinct living people;
- Owner-observed ordinary aliveness;
- Owner gate / merge readiness.

The project should retain this result as a new baseline falsifier, then move outward into a different resident-owned causal dimension rather than polishing the same promise loop indefinitely.
