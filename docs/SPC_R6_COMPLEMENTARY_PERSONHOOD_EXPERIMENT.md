# SPC R6 — Complementary Resident Personhood Experiment

Status: **BINDING CURRENT R6 EXPERIMENT CONTRACT**
Date opened: **2026-09-21**
Parent qualified R5 head: `09178c66a4bc63d982f068bec0ebfe8beeba972a`
Real-Luna R5 evidence source: `3122baabd64fdf57cc178f29ee98b81b9b8071af`

## 1. Why R6 exists

The post-stress campaign has now defended the machinery that a living resident needs:

- zero-provider local life;
- private relevance / semantic-pressure lifecycle;
- first causal-personhood effect from resident-owned continuity/history;
- dense authored material affordances with legitimate non-action;
- sparse higher cognition with explicit origin and settlement;
- provider latency without freezing World life;
- stale/newer-attention rejection;
- decline/defer/clarify/error homeostasis;
- one real GPT-5.6 Luna escalation through the same local admission membrane;
- a literal hard experiment budget that blocked a second upstream request.

Those are foundations.

They are not the product goal.

The Owner goal remains five residents who are players of their own lives:
- own matters and goals;
- preferences and aversions;
- refusal and self-interest;
- relationships and obligations;
- habits and ordinary continuity;
- comfort, irritation and curiosity where causally earned;
- ownership/control/attachment where causally earned;
- ability to ignore the player;
- behavior that remains coherent when nobody is directing them.

R6 returns the campaign from provider mechanics to that problem.

## 2. Current personhood truth

The five-resident authored self layer is still:

`ResidentLifeSelfContext = { role: string, drives: string[] }`.

This prose is useful context.

It is **not** causal authority for:
- preference;
- aversion;
- trust;
- resentment;
- ownership;
- relationship change;
- habit;
- self-interest;
- future obligation.

R3 explicitly qualified this boundary.

R3 also established something more useful:

> existing resident-owned continuity/history is already part of the causal self.

Therefore R6 should first make real private history increasingly consequential before inventing a parallel personality simulation.

## 3. New failure exposed by real Luna

The first bounded real-Luna R5 judgement said:

`Tak, zostanę chwilę przy stole.`

The locally admitted intent was only:

`communicate_actor`.

That matter resolved after the sentence was factually delivered.

No durable resident state represented:
- staying;
- a promise to stay;
- a social obligation created by the promise;
- a future check that Mira actually remained.

Therefore current semantic plumbing can allow:

`speech act -> future promise in text -> speech delivered -> matter resolved -> promise disappears from causal resident state`.

This is a concrete resident-personhood failure.

Do not solve it only by telling the model:
“avoid promises”.

That would hide missing causal representation behind prompt behavior.

## 4. R6 core hypothesis

> **Ordinary personhood can emerge from persistent private causal history that changes what matters to the resident and what commitments continue to exist, rather than from roleplay prose or repeated provider invention.**

A useful R6 personhood fact must have:

1. an inspectable origin;
2. bounded structured meaning;
3. private resident ownership;
4. lifecycle / revision / termination semantics;
5. at least one observable effect on significance, choice or durable commitment;
6. no direct body authority;
7. no resident-name special case.

## 5. Complementary residents

### Mira — ordinary personhood pressure

Use Mira to attack:
- personal stake;
- refusal / acceptance for resident-owned reasons;
- social commitment fidelity;
- preferences/aversions only when causally earned;
- ordinary non-action;
- relationship/history effects;
- personal cost/inconvenience when it becomes concrete.

### Janek — material/continuity counterpressure

Use Janek to ensure personhood work does not break:
- grounded material work;
- interruption/return;
- stale private material knowledge;
- reacquisition;
- exact factual execution;
- useful mundane competence.

Both must use shared generic architecture.

No:
`if (residentId === "resident.mira")`.

## 6. R6-A — resident-originated standing social commitment

Status: **QUALIFIED / BOUNDED PASS — 2026-09-21**

Qualification record:
`docs/SPC_R6A_STANDING_SOCIAL_COMMITMENT_PASS.md`

R6-A first made the R5 future-promise gap executable, then introduced the smallest bounded private-state primitive justified by that failure.

The qualified causal chain is:

1. one exact addressed Ida speech creates one resident-owned semantic attempt;
2. a provider-shaped decision accepts one grounded `communicate` matter/run;
3. a standing-commitment capability can be prepared only while that exact communicate run is live;
4. provider completion and semantic preparation alone create no standing history;
5. Mira factually speaks the promised sentence through World authority;
6. the communicate run resolves from exact factual outcome provenance;
7. only then can the prepared capability materialize one private `standing_social_commitment`;
8. that matter owns no run and no body authority;
9. it persists while Ida physically leaves;
10. the same later Ida sight-enter becomes resident-relative semantic pressure because of that exact private history;
11. private release resolves the commitment and locally invalidates the derived pressure without manufacturing a World fact;
12. an equivalent later Ida sight-enter is again observation-only.

This qualifies one bounded causal-personhood effect beyond provider prose and beyond the terminal communicate obligation.

It does **not** qualify:
- full R6;
- general promise understanding;
- automatic inference of commitments from arbitrary speech text;
- preferences, aversions, trust, resentment, habit or self-interest;
- cross-resident behavioral genericity of this new primitive;
- ordinary Owner-observed aliveness.

Authored `role/drives` remain non-authoritative exactly as established by R3.

## 7. R6-B — native life-intent standing continuation

Status: **QUALIFIED / BOUNDED PASS — 2026-09-22**

Qualification record:
`docs/SPC_R6B_NATIVE_STANDING_SOCIAL_CONTINUATION_PASS.md`

R6-B removes the experiment-owned `prepare-standing` / `materialize-standing` knowledge from the normal causal path.

An accepted `communicate` life-intent may now explicitly declare only that the exact future communication is intended to continue as standing social responsibility:

`standingSocialCommitment = { goal }`

That declaration is resident-owned semantic intent after local admission, but it is still inert with respect to standing history and body authority.

The durable standing matter can arise only after:

1. one exact private origin reason survives normal life-intent admission;
2. one grounded `communicate_actor` matter/run exists;
3. the standing declaration was already part of that resident-owned communicate meaning;
4. the exact resident speech occurs factually in World;
5. the exact source run outcome reconciles successfully.

The durable commitment wording is **the exact factual communication text** carried by the source matter. There is no second provider-authored promise paraphrase that could disagree with what the resident actually said.

Text alone still does not imply a standing commitment. An ordinary communication containing future-sounding language but lacking the explicit continuation declaration remains ordinary communication and creates no standing history.

R6-B preserves the qualified R6-A lifecycle/significance effects:
- run-free persistence;
- no provider heartbeat;
- later actor-relative significance;
- private release;
- observation-only equivalent contact after release;
- no direct body authority.

R6-B is not full R6 and does not yet qualify cross-resident behavioral genericity.

## 7.1 R6-C — cross-resident generated-history integration

Status: **DETERMINISTIC CAUSAL EVIDENCE GREEN — NOT BROWSER-QUALIFIED**

The second-resident falsifier now runs the native standing-history chain through Oren rather than Mira:

`Nela factual request -> Oren normal cognition admission -> Oren factual self speech -> Oren standing history`.

That exact runtime-generated Oren matter is then carried into a later cognition episode caused by factual Ida speech. The next `ResidentCausalCognitionRequest.context.life` contains the generated standing commitment, and the same Worker sanitizer used by the live endpoint accepts that exact context.

This proves more than resident-name-generic code:
- different resident identity and namespace;
- different counterparty;
- exact private origin;
- factual self speech before standing history;
- durable wording equal to factual speech;
- run-free standing matter;
- generated history present in later higher-cognition context;
- Worker boundary preserves it without fixture-owned reconstruction.

The bounded real-Luna history-choice twin remains a separate evidence plane. It used a canonical synthetic twin context rather than sending the runtime-generated object over the network. Its observed choice contrast is real evidence, but those two facts must not be conflated into a single end-to-end live-provider run.

Browser-qualified Oren/Nela genericity also remains **UNPROVEN**. Browser Evidence #811 is green on code source `da76300c78236c6c8d87277159c0d105f987f62a`, but the dedicated R6 Chromium specimen still exercises the Mira/Ida native standing-commitment path.

## 7.2 R6 standing lifecycle — exact counterparty release

Status: **DETERMINISTIC BOUNDED EXTENSION GREEN — NOT A BROAD SOCIAL-CONTRACT PASS**

R6 exposed a material lifecycle failure after history-sensitive choice became real: an open standing commitment could influence cognition but normal higher cognition had no legal way to end that existing matter. A resident could understand “Nela released me” yet remain causally stuck with the old responsibility.

The smallest earned lifecycle seam is now `release_standing`.

It is deliberately narrow:
- it targets one exact open run-free `standing_social_commitment` already present in resident `life`;
- the selected cognition origin must be exact heard speech;
- the factual speaker must be that standing matter's own counterparty;
- the speech must be addressed to the resident;
- ordinary stale-life admission guards still apply;
- the transition creates no new task, run, body authority or World fact;
- it resolves only resident-private responsibility and records release evidence;
- a different actor cannot release another actor's standing commitment.

On code source `da76300c78236c6c8d87277159c0d105f987f62a`, Check #1587 passes **260 / 260 test files and 955 / 955 tests**, including:
- the original Oren→Nela generated-history chain;
- generated history reaching later cognition/Worker context;
- factual Nela release speech resolving that same generated Oren matter through the normal cognition lane;
- body state unchanged by release;
- exact release evidence retained in committed/recent history;
- wrong-counterparty release rejected.

Browser Evidence #811 also remains green as regression evidence, including the dedicated Mira R6 specimen and the earlier R1–R5 browser vetoes. It does **not** independently qualify this Oren release path.

This seam does not solve:
- automatic time-based expiry of “for a while”;
- arbitrary fulfilment inference;
- third-party release;
- general contract law/social norm understanding;
- whether a resident should release for every semantically plausible utterance;
- Owner-observed ordinary life.

## 7.3 R6 endogenous history-after-outcome choice

Status: **MATERIAL BOUNDED POSITIVE FINDING — NOT R6 PASS**

Canonical finding:
`docs/SPC_R6_ENDOGENOUS_ORDINARY_PERSONHOOD_FINDING.md`

The new falsifier removes the fresh-command dependency from the decision point.

Two exact contexts are generated by the normal runtime after Oren factually completes the same workshop matter. Both have:
- one `activity_completed` reason from the same factual outcome;
- no fresh `heard_speech` reason;
- idle body;
- current region workshop;
- no authored `self` / drives.

The intended semantic difference is earlier resident-owned continuation history. Both Oren variants factually spoke the same return sentence to Nela; only the history branch made it durable as an active run-free standing responsibility.

R2's no-echo policy remains intact. Successful completion does not globally create semantic work. This R6 specimen explicitly opts one exact factual outcome into `ResidentLifeOutcomeReviewBridge`.

The first live attempt, run #21 at `4861abf68bf24b8e0c5bb5b7e8aea62e4e723a44`, made two real Luna inferences but failed at the provider/local contract boundary. Those calls are part of experiment cost/history but are not positive semantic evidence.

After repairing that apparatus mismatch, canonical Live Provider run #22 at `592a19ecdb34598968690812310996aef8f2b0f7` produced:

- control: `decline` — the workshop work is complete and no distinct new continuing matter is grounded;
- history: `accept travel -> commons` — specifically because the earlier standing commitment requires returning to Nela.

Run #22 made exactly two calls and no within-run semantic retry.

The exact stored history proposal is replayed through normal local admission and execution. Oren factually returns from workshop to commons without fresh speech.

This is the first bounded evidence of:

`private causal history -> no-fresh-command semantic choice -> factual World continuation`.

It is not spontaneous cognition in total quiet and must not be used to reintroduce a periodic provider heartbeat.

## 7.4 R6 factual standing fulfilment

Status: **LIVE-SEMANTIC + DETERMINISTIC LIFECYCLE GREEN — BOUNDED**

The successful endogenous return exposed another sticky-history failure: physical return alone did not semantically fulfil the standing promise.

That separation is correct, but normal cognition needed one legal bounded lifecycle decision.

`complete_standing` now allows higher cognition to propose fulfilment of one exact open run-free standing commitment only when the selected current origin is one exact `activity_completed` factual task outcome.

Local authority proves:
- the exact standing target exists and is still open;
- the exact factual outcome exists;
- the causal origin is exact.

Higher cognition judges whether that outcome semantically satisfies the responsibility.

The runtime-generated post-return context is frozen in:
`evidence/r6-endogenous-standing-fulfillment-context.json`.

Live Provider run #23 at `9aff2659118a780cc9b82014d5666537832842e3` used exactly one GPT-5.6 Luna call and zero retries. Luna selected:
- `complete_standing`;
- exact standing matter `matter-social-commitment:471529c708005f28`;
- exact factual-return reason.

The exact live proposal is replayed through normal local authority:
- standing matter resolves;
- `resident_fulfilled_social_commitment` evidence records the factual return run/outcome;
- no new body/task/World authority is manufactured.

Integrated source `32bae5f34982ef942ef6d3ea58fc0000212f79ec`:
- Check #1603 PASS — **263 / 263 files, 961 / 961 tests**, typecheck/build/preview PASS;
- Browser Evidence #827 PASS as regression evidence.

Browser #827 still does not qualify Oren/Nela genericity: the dedicated R6 Chromium specimen remains Mira/Ida.

## 8. Anti-cheat rules

R6 FAILS if apparent personhood mainly comes from:

- adding personality adjectives to the prompt;
- adding more verbose `drives`;
- random action variation;
- per-resident hard-coded utility weights;
- a generic needs/Sims meter without a concrete failure;
- provider-authored “I like / hate / trust / promised” prose with no independent origin;
- treating one speech string as durable obligation without lifecycle;
- creating a matter for every emotion or preference;
- making personhood state directly seize body authority;
- measuring success by dialogue quality alone;
- treating “five different names” as five different people.

## 9. Evidence planes

Keep separate:

- **ORIGIN** — what exact event/history created the private personhood fact?
- **LIFECYCLE** — open, revised, fulfilled, released, superseded?
- **SIGNIFICANCE** — does identical current evidence matter differently because of history?
- **CHOICE** — does a legal ambiguity expose causally different support?
- **NON-ACTION / REFUSAL** — can the resident legitimately decline or ignore?
- **BODY AUTHORITY** — self state never moves/speaks by itself;
- **WORLD OUTCOME** — actual actions remain factual World consequences;
- **PROVIDER ROLE** — provider may judge conflict but may not invent why the resident cared;
- **COMPLEMENTARY JANek** — material competence/continuity does not regress.

## 10. Promotion direction

Do not freeze full R6 PASS criteria yet.

The first milestone should at minimum demonstrate one new ordinary-personhood fact beyond open-matter responsibility that:

- existed before the decision it influences;
- has exact private causal origin;
- persists across time without provider heartbeat;
- changes significance or a real legal choice;
- can terminate/change without deleting history;
- survives deterministic and browser evidence;
- is generic across residents.

Only then decide whether the next pressure should be:
- preference/aversion;
- relationship history;
- ownership/control;
- habit;
- personal inconvenience / self-interest;
- another Owner-relevant dimension.

## 11. Immediate work order

Completed / retained within scope:

- R5 qualified sources remain frozen;
- R6-A and R6-B remain bounded qualified mechanism passes;
- Oren→Nela standing history is deterministic/domain green and crosses the Worker boundary;
- earlier direct-request real-Luna history-sensitive choice remains a bounded positive finding;
- exact counterparty release remains deterministic/domain green;
- runtime-generated no-fresh-command control/history contexts are equality-bound to executable fixtures;
- canonical endogenous Live Provider run #22 observes coherent control non-action versus history-caused return to Nela;
- the exact history proposal from run #22 survives local admission and becomes factual World travel back to commons without fresh speech;
- `complete_standing` provides one narrow factual-outcome-based fulfilment path without body/World authority;
- runtime-generated post-return fulfilment context crosses the Worker boundary;
- Live Provider run #23 uses one Luna call and selects exact `complete_standing`;
- the exact run-#23 proposal survives local authority and resolves the original standing responsibility;
- Check #1603 is green on integrated source `32bae5f34982ef942ef6d3ea58fc0000212f79ec`: **263 / 263 files, 961 / 961 tests**, typecheck/build/preview PASS;
- Browser Evidence #827 is green on the same code source as regression evidence.
- stage-closure hardening source `2c7adf9415eefff5f2c7236ead7a59b10a3f8ab0` adds explicit stale-`complete_standing` rejection evidence; Check #1605 passes **263 / 263 files, 962 / 962 tests**, and Browser Evidence #829 passes with no production/runtime semantic change after `32bae5f3...`.

Evidence hygiene:

- run #21 was a real two-inference **apparatus/contract failure**, not positive semantic evidence; do not hide its cost or reuse its rejected proposals;
- “two calls, no retry” refers to canonical run #22 itself, not the entire campaign;
- the endogenous reflection opportunity is an explicit R6 opt-in from a factual outcome; R2's global expected-success suppression remains unchanged;
- initial standing-history construction in this paired specimen is deterministic experiment setup, not a claim that Luna autonomously authored Oren's entire biography;
- the real endpoint receives exact runtime-generated contexts through captured/equality-bound fixtures, not a single uninterrupted browser process;
- browser-qualified Oren/Nela genericity remains **UNPROVEN**;
- one paired Luna sample is not a stochastic behavior distribution;
- full R6, five distinct living people and Owner-observed ordinary personhood remain **UNPROVEN**.

Current order:

1. preserve the new endogenous finding as a baseline falsifier; do not rerun it merely to accumulate green samples;
2. stop deepening the same explicit-promise dimension unless a new concrete failure demands it;
3. choose the next personhood pressure from a **different resident-owned causal class** — e.g. factual unfinished business, learned preference/aversion, relationship history, habit or concrete self-interest — without pre-building a generic personality/needs system;
4. keep coherent non-action as a valid success outcome; do not force “autonomy” to mean constant activity;
5. retain browser Oren/Nela genericity as honest promotion-plane debt and obtain it or explicitly revise the promotion contract before a claim that requires it;
6. preserve Janek/material continuity, R1 homeostasis, R2 semantic metabolism and R5 provider safety as vetoes;
7. do not prepare an Owner gate or merge PR #148 by CI inertia.

The next research question is now:

> **Can prior factual life cause coherent endogenous choice or non-action for a resident-owned reason that is not simply an explicit social promise or obligation?**

## 12. Owner-observed target

R6 is moving toward the question that matters more than any green provider trace:

> If nobody tells this resident what to do, does their accumulated private life make them behave like **someone** rather than like a safe orchestration pipeline?

Do not claim that target has been reached until Owner-observed ordinary life supports it.
