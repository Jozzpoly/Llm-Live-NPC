# SPC R6-A — Resident-Originated Standing Social Commitment PASS

Status: **QUALIFIED BOUNDED PASS**
Qualified: **2026-09-21**
Repository: `Jozzpoly/Llm-Live-NPC`
Qualified source SHA: `ed97256d35c0c582f1d0c6133ea70764c9989290`
Branch: `recovery/spc-post-stress-complementary-personhood-r6`
PR: **#148 — draft/open**

Parent authority:
- `docs/SPC_POST_STRESS_RECOVERY_PROGRAM.md`
- `docs/SPC_R6_COMPLEMENTARY_PERSONHOOD_EXPERIMENT.md`

## 1. Qualified claim

R6-A earns only this bounded claim:

> A resident's own factually executed speech can create one private standing social commitment with exact causal origin and lifecycle; that run-free history can later make otherwise equivalent current evidence matter differently, and releasing it can remove that significance without inventing a World fact.

This is the first R6 causal-personhood fact beyond the already-open responsibility represented by the source communicate matter.

It is not a full personhood or R6 PASS.

## 2. Exact evidence

### Repository

GitHub Check **#1559 — PASS** on the exact qualified source:

- **256 / 256 test files PASS**;
- **947 / 947 tests PASS**;
- TypeScript PASS;
- production Vite builds PASS;
- self-contained preview dry-run PASS.

### Real Chromium

Browser Evidence **#783 — PASS** on the same source.

Browser:
`Chrome/152.0.7977.82`

The dedicated R6-A campaign passed every asserted boundary, including:

- only Mira + Ida, no player and no pre-existing standing commitment;
- one exact addressed-speech semantic attempt;
- provider completion inert until World admission;
- standing capability prepared only while the exact communicate run is live;
- factual promise speech exactly once before standing history exists;
- standing commitment materializes only after factual self speech;
- standing commitment is active and run-free;
- Ida can leave while the private commitment persists without taking Mira's body;
- Ida's real sight re-entry becomes significant because of the exact standing history;
- private release resolves the matter and invalidates derived significance without a World occurrence;
- equivalent second re-entry after release is observation-only;
- all promoted checkpoints deterministic across two real-Chrome reloads;
- zero real provider/API network requests in the deterministic fixture;
- zero uncaught runtime exceptions;
- screenshots non-empty and canonically read-only.

All earlier browser vetoes R1 through R5-C also passed on the same source.

## 3. Causal origin boundary

The new state is not inferred from a sentence after the fact.

Authority is two-phase:

1. an opaque capability is prepared while one exact grounded `communicate_actor` matter/run is live;
2. materialization is legal only after matching factual resident speech exists and exact source-run outcome provenance proves factual delivery.

A fabricated occurrence, forged capability, terminal source without prior preparation, mismatched actor/text or duplicate materialization fails closed.

Therefore provider prose or raw speech text alone is not standing-personhood authority.

## 4. Private continuity and body boundary

The materialized matter has structured intent:

`standing_social_commitment`

with:
- one exact counterparty actor;
- bounded resident-owned meaning;
- exact origin evidence;
- active/resolved lifecycle.

It has no active run and does not claim body focus.

Persistence across committed-state reconstruction is covered by deterministic evidence.

Private release:
- advances resident semantic history;
- resolves the standing matter;
- creates no external World fact claiming objective fulfilment or counterparty release.

## 5. Significance twin

Deterministic twin evidence presents the same later Ida sight-enter to:

- a resident carrying the open standing commitment;
- an otherwise equivalent resident history without it.

Only the first gains semantic pressure.

After release, an equivalent later sight-enter becomes observation-only again.

This is the bounded R6 personhood result that matters:

> identical current evidence can have different resident-relative meaning because of accumulated private causal history.

## 6. What remains unqualified

R6-A does **not** prove:

- full R6 ordinary personhood;
- general natural-language promise understanding;
- automatic promise extraction from speech;
- normal life-intent creation of standing commitments;
- cross-resident behavioral qualification of the new primitive;
- preferences or aversions;
- trust or resentment;
- relationship models;
- ownership or attachment;
- habit;
- self-interest;
- refusal/non-action driven by accumulated personal history;
- five residents behaving like distinct persistent people;
- Owner-observed aliveness.

The new browser specimen still uses experiment-owned `prepare-standing` / `materialize-standing` actions. That is evidence apparatus around a qualified primitive, not yet normal resident-life expressivity.

## 7. Current frontier

The next question is not whether to invent a personality system.

It is:

> Can the ordinary resident life-intent/admission path explicitly carry the bounded semantic fact that one factual communication is intended to create a standing social commitment, without inferring promises from text and without letting provider output become its own causal authority?

The current strict `ResidentLifeIntentProposal` accepts one `ProposedActivity` and has no native representation for that continuation.

That gap should be characterized before extending the contract.

Cross-resident genericity remains a separate required falsifier before promoting the broader first R6 milestone.
