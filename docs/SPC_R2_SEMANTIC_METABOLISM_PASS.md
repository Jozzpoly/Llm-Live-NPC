# SPC R2 — Semantic Metabolism PASS

Status: **SEMANTIC-METABOLISM PASS**
Qualified source: `f0cd892a7915dbe5a382bcdd60966415e4544604`
Date: **2026-09-20**
Parent authority: `docs/SPC_POST_STRESS_RECOVERY_PROGRAM.md`
Experiment contract: `docs/SPC_R2_SEMANTIC_METABOLISM_EXPERIMENT.md`

## Verdict

R2 has earned its bounded claim:

> **A resident can perceive a noisy causal world, keep living locally, and retain only the small explainable set of semantic pressures that actually deserve unresolved cognition. Raw perception and passage of time no longer own provider-shaped cognition authority by default.**

This is not a living-SPC/product PASS.

R2 does **not** prove:
- causal personhood;
- rich ordinary autonomous life;
- dense authored affordances;
- real-provider judgement quality;
- long-duration five-resident identity/coherence;
- production budget safety;
- the final local-brain architecture.

Those remain later recovery gates.

## Exact machine evidence

### Repository qualification

Exact source `f0cd892a7915dbe5a382bcdd60966415e4544604`:

- GitHub Check run **#1377 — PASS**
- full typecheck / domain tests / Vite build passed;
- deterministic noisy-world replay is included in this exact source.

Browser regression evidence:
- Browser Evidence **#603 — PASS** at `db35d6d556b6d6b06f62a5a66d073b75e5165fd1`;
- the only later source change through `f0cd892...` was deterministic R2 test evidence, not the qualified browser runtime paths.

The browser campaign retained:
- zero-provider local-life evidence;
- addressed local interruption + exact return;
- missing-crate checked-absence epistemics;
- embodied search/recovery;
- no-provider R1 browser invariant.

## R2 contract audit

### 1. Raw percept ingestion no longer implies scheduler pressure — PASS

`ResidentSemanticPressureGate` now separates private observation from explicit unresolved pressure.

Observation-only by default includes:
- ambient/unaddressed speech;
- speech from a privately known actor without an established resident-relative relevance;
- actor sight enter/exit;
- authored region transition;
- raw interaction/system occurrences.

These observations remain private evidence. They are not erased or treated as unperceived.

### 2. High-volume low-context observations do not create an equally large unresolved set — PASS

`r2-semantic-pressure-metabolism.test.ts` and
`r2-noisy-world-homeostasis.integration.test.ts` qualify:
- many heard ambient utterances;
- recognized-actor background speech;
- visibility churn;
- real `emitInteraction()` World occurrences.

The resident preserves observation truth while unresolved semantic pressure remains empty unless a separate relevance/discrepancy mechanism promotes it.

### 3. Genuine addressed/discrepancy pressure still promotes — PASS

Two complementary mechanisms survive the filter:

- addressed speech remains an urgent `heard_speech` reason;
- Janek's physically checked absence of the familiar crate becomes one explicit resident-relative `uncertainty` reason tied to exact `checked_absence` evidence.

R2 therefore did not achieve quiet by making residents blind.

### 4. Local body handling does not falsely settle semantic content — PASS

The zero-provider Mira specimen can:
- interrupt;
- turn toward the participant;
- answer `Tak?`;
- hold briefly;
- return to the exact material matter;

while the original addressed semantic pressure remains unresolved.

Body response and semantic settlement remain separate facts.

### 5. Pressure lifecycle is explicit — PASS

`ResidentSemanticPressureLifecycle` exposes per-reason:
- `pending`;
- `in_flight`;
- `settled`;

plus inspectable transitions such as:
- promoted;
- superseded;
- stale ignored;
- dispatched;
- requeued;
- retained;
- settled.

`defer` / `clarify` retain an existing reason with an explicit eligibility boundary instead of manufacturing a new timer reason.

### 6. Settled/superseded evidence does not resurrect — PASS

Reason identity is monotonic by causal tick.

Executable adversaries prove:
- an older requeue cannot overwrite newer evidence with the same coalesced identity;
- settling an older in-flight version cannot settle a newer version that arrived during provider latency;
- unrelated siblings in one cognition batch are retained instead of disappearing when one origin is settled.

This closes both stress-test failure families:
- event necromancy;
- semantic amnesia.

### 7. Expected consequences do not echo into new semantic work — PASS

Generic causal execution now distinguishes:
- successful expected terminal outcome: factual resident/kernel history, no automatic fresh semantic pressure;
- blocked/non-terminal outcome: real discrepancy, eligible for outcome review.

Outcome reflection remains available as an explicit opt-in mechanism for experiments that genuinely require semantic follow-up. It is no longer a global `task -> cognition -> task` heartbeat policy.

### 8. Passage of time alone does not manufacture semantic work — PASS

The scheduler schedules already-established unresolved reasons.

`quiet_review` is no longer fabricated when pending pressure is empty and is no longer accepted as a causal self-origin for a new matter.

Local-maintenance deadlines may still exist as diagnostics/maintenance timing. They do not create semantic discrepancy.

### 9. Local life survives noisy perception — PASS

The deterministic noisy-world campaign keeps Mira's real basket matter progressing through:
- ambient speech;
- raw interactions;
- sight churn;
- background social evidence;
- addressed interruption;

and reaches the same factual material consequence and legitimate quiet state.

### 10. Mira and Janek use the same core metabolism/lifecycle — PASS

There is no resident-name branch in the semantic metabolism core.

Mira pressures:
- ordinary social/noisy life;
- addressed contact;
- local body interruption;
- quiet.

Janek pressures:
- stale material knowledge;
- checked absence;
- persistent discrepancy.

Both enter the same resident runtime pressure/lifecycle substrate.

### 11. Deterministic noisy-world replay is stable — PASS

At `f0cd892...` the same full noisy local-life campaign is executed twice and required to produce the same:
- World tick/end state;
- matter terminal state;
- material-object location;
- local attention;
- unresolved reason set;
- lifecycle snapshot;
- local-decision trace;
- semantic-pressure decision signature.

### 12. No provider is required for R2 qualification — PASS

The core noisy-world and Mira/Janek discrepancy qualification is fully local/deterministic.

Provider tests below are additional post-stress falsifiers, not prerequisites used to hide a weak local metabolism layer.

## Additional system-level evidence

### Five-resident quiet provider homeostasis

A unified five-resident runtime with a bounded fake semantic provider:
1. settles every explicit initial pressure;
2. reaches an empty unresolved/transport state;
3. advances another **6000 World ticks**;
4. produces **zero additional provider requests**.

Janek's legitimate authored idle remains completely request-free.

This directly falsifies the historical unconditional quiet-heartbeat behavior.

### Genuine five-resident burst

From homeostasis:
- five physically local, addressed contacts are delivered to the five residents;
- all five produce provider requests in the same authoritative World tick;
- all exact origins are `heard_speech`, not timer pressure;
- after bounded semantic settlement the runtime returns to homeostasis;
- another **3600 quiet ticks** produce no echo traffic.

Therefore R2 homeostasis is not achieved by suppressing legitimate cognition intensity.

## Architectural result

The current boundary is now:

`World / private evidence`
→ `resident-local observation & causal explanation`
→ **explicit unresolved semantic pressure**
→ `pressure lifecycle`
→ `scheduler opportunity`
→ optional later provider escalation.

The scheduler no longer decides what matters.
Raw occurrence type no longer decides what matters.
Passage of time no longer decides what matters.

A separate resident-relative mechanism must be able to explain why an observation becomes unresolved pressure.

## Important remaining debt

R2 intentionally stops before answering the harder personhood question:

> **Why should this particular resident care?**

Current five-resident `role` / `drives` are still mostly authored provider-facing prose.
ResidentMind contains useful private beliefs/concerns and the continuity kernel contains real matters/history, but there is not yet a sufficiently causal resident-self substrate for:
- obligations;
- preferences/aversions;
- relationships;
- ownership/control;
- habits;
- inconvenience/effort;
- personal unfinished business;
- selective care.

That is the next recovery pressure.

## Promotion

R2 is closed as **SEMANTIC-METABOLISM PASS**.

Next gate:

> **R3 — causal personhood: resident-relative significance must increasingly arise from persistent private causal state rather than roleplay prose or an unconstrained provider reason string.**
