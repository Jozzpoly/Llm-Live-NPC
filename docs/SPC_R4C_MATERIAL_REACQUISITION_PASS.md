# SPC R4-C — Legal Material Reacquisition PASS

Status: **QUALIFIED PASS — CHANGED-WORLD / LOCAL REACTIVATION**
Date: **2026-09-20**
Repository: `Jozzpoly/Llm-Live-NPC`

## 1. Exact qualified source

Code / runtime evidence source:

`423c9ba1eaafdc9064136ed0a1a3c71b7b19b2ca`

Immutable source pointer:

`recovery/spc-post-stress-dense-affordance-r4c-qualified-source`

R4-B qualified source:

`5548231763718f868186dd5c07877c65969f7f88`

Parent R3 checkpoint:

`27ee3e1d1ca15a528c239d6df23563a2f820a015`

Development line:

`recovery/spc-post-stress-dense-affordance-r4`

Draft PR:

`#146 — recovery: R4 dense authored affordance surface`

This report qualifies the exact source SHA above. Later documentation commits are not the tested runtime source.

---

## 2. Promotion decision

**R4-C PASS, within the bounded R4 claim.**

The changed-World / causal-reactivation requirement is defended:

> A material fact can change outside the resident's knowledge, remain unresolved through legitimate quiet, later become legally visible, update resident-private knowledge on an ordinary local-life tick, become relevant to exactly one existing blocked material matter, settle the obsolete checked-absence pressure, restore a fresh exact local run and complete the same durable goal without provider cognition.

The provider does not say “the crate is back”.

The blocked matter does not poll hidden World truth.

The scenario controller does not directly say “now pick up A”.

The causal path is:

`external World action`
→ `World truth changes`
→ `resident still knows old truth`
→ `ordinary resident-local material sample`
→ `private reacquisition`
→ `matter-relative relevance`
→ `semantic evidence revision`
→ `old absence pressure settled`
→ `fresh exact run`
→ `existing pickup competence`
→ `World outcome`
→ `matter resolved`

R4 overall is still **NOT PASS**.

R4-D — second resident / Mira falsifier — remains mandatory.

---

## 3. Verification

### Repository Check

GitHub Actions:

- workflow: `Check`
- run: **#1477**
- run id: `35528310184`
- source SHA: `423c9ba1eaafdc9064136ed0a1a3c71b7b19b2ca`
- result: **SUCCESS**
- test files: **247 / 247 PASS**
- tests: **929 / 929 PASS**
- build gates: **PASS**

### Browser Evidence

GitHub Actions:

- workflow: `Browser Evidence`
- run: **#703**
- run id: `35528310186`
- source SHA: `423c9ba1eaafdc9064136ed0a1a3c71b7b19b2ca`
- result: **SUCCESS**
- Chrome: **152.0.7977.82**

All prior browser vetoes remained green:

- canonical browser evidence;
- manual World control;
- missing-crate epistemic boundary;
- missing-crate embodied recovery;
- participant interruption / exact return;
- Ida social delivery/private-hearing join;
- R1 zero-provider local life;
- R4-B dense-workshop alternate-life path.

The R4 dense-workshop browser gate now also includes the R4-C reveal / reacquisition / reactivation / pickup path.

No provider/API request is allowed by the R4 browser oracle.

---

## 4. Why R4-C existed

R4-B ended with:

- A — familiar workshop crate — active but blocked after checked absence;
- B — basket return matter — resolved;
- C — background stool — irrelevant;
- Janek legitimately quiet;
- no provider cognition manufacturing continuation.

The next required question was:

> If World truth later changes so A becomes observable again, can the resident recover that fact through ordinary private perception and continue the same existing matter locally?

Initial characterization exposed two separate gaps.

### Sensory gap

`ResidentMaterialKnowledge` was mainly sampled inside active material executors.

A quiet resident could therefore physically have a recognized object inside nominal sight while the material extension remained stale.

### Meaning gap

Continuity had structured durable intents for:

- `travel_region`;
- `communicate_actor`.

The material matter still existed only as text such as “retrieve the familiar crate”.

A correct local relevance system must not parse `semanticCourse` prose or rely on a hidden fixture map to learn which object the matter concerns.

---

## 5. Minimal semantic addition

R4-C added one deliberately narrow resident-owned matter intent:

`acquire_material_object`

Fields:

- `goal`;
- `objectId`.

It means only:

> this continuing resident matter is about acquiring this recognized material identity.

It is **not**:

- a generic affordance language;
- an action planner;
- a search plan;
- an inventory system;
- ownership;
- repair/use/crafting semantics;
- a World query;
- a provider schema expansion.

Concrete pickup/search/navigation methods remain separate execution.

---

## 6. Material reacquisition relevance seam

New reusable seam:

`ResidentMaterialMatterRelevanceBridge`

Its input is resident-private observation before/after ordinary material sampling plus the resident life view.

It does not read hidden object truth.

It recognizes a material observation as locally reactivatable only when:

1. the same recognized object transitions from `currentlyVisible=false` to `true`;
2. exactly one resident matter is:
   - active;
   - currently without an active run;
   - structurally `acquire_material_object`;
   - targeted at that exact `objectId`;
   - carrying a factual blocked task outcome.

When this is true it:

- records `material_reacquired` resident evidence;
- advances that matter's semantic context;
- settles the exact older material-absence pressure.

It does **not**:

- bind a run;
- claim the body;
- choose among multiple matching matters;
- search for the object;
- create a new matter.

A dedicated ambiguity test proves that if two blocked matters target the same reacquired object identity, the bridge returns `ambiguous` and changes neither matter.

---

## 7. Exact qualified causal history

### 7.1 Hidden period

After the initial R4-B adversarial relocation, A remains outside legal sight through:

- A's stale-position inspection;
- checked absence;
- B's pickup/place work;
- the later quiet interval.

The current source uses real World motion constraints rather than assuming an exact fixture coordinate.

The hidden position in Browser #703 was approximately:

`(1710.61, 1058.74)`

Janek's private remembered A position remained:

`(500, 500)`

with `currentlyVisible=false`.

### 7.2 Legitimate blocked state

A remains:

- status: `active`;
- active run: none;
- semantic revision: 2;
- semantic evidence: `checked_absence`;
- durable material intent: `acquire_material_object(crate.r4.remembered)`.

The checked-absence pressure remains unresolved while the discrepancy remains real.

B is resolved.

C remains untouched.

### 7.3 Explicit later World change

After 600 quiet ticks, the evidence harness performs one explicit external World boundary.

Browser #703:

- reveal tick before: **1907**;
- reveal tick after: **1908**;
- A World position changes from approximately
  `(1710.61, 1058.74)`
  to
  `(1550, 650)`.

At the exact post-World-change boundary:

- A is now physically nearby;
- A matter is still blocked/unrun;
- Janek's private A knowledge still contains the old checked-absence state;
- the old uncertainty pressure still exists.

Therefore external World mutation does **not** directly mutate resident knowledge.

### 7.4 Next ordinary local tick

On the next resident-local tick:

Browser #703 tick: **1909**

Material sampling legally observes A.

Private semantic evidence becomes:

- kind: `material_reacquired`;
- tick: 1908;
- summary: recognized A became privately visible at `(1550,650)`.

A advances to semantic revision **3**.

Fresh exact run:

`run.janek.r4.pickup-reacquired-crate`

is bound at revision 3.

Existing body arbitration gives it focus.

The old checked-absence semantic pressure is settled locally.

Browser evidence reports cognition pressure count:

`0`

at the reactivated checkpoint.

No provider request supplied the decision.

### 7.5 Factual completion

Janek uses the already-earned material pickup executor.

Factual success outcome:

- source run: `run.janek.r4.pickup-reacquired-crate`;
- outcome tick: **2013**;
- summary: `succeeded: picked up crate.r4.remembered after legal private reacquisition`.

Browser checkpoint at tick **2017**:

- A matter: `resolved`;
- A World state: held by `resident.janek`;
- B remains resolved at its destination;
- C remains unchanged;
- body focus: none;
- pending cognition pressure: 0.

The complete resident action-fact history is therefore:

1. B pickup;
2. B place;
3. reacquired A pickup.

No unrelated material fact becomes a chore.

---

## 8. Evidence-host discipline

R4-C needed an explicit later external World change in real Chromium.

The research host gained an evidence-only seam:

`scenarioAction(actionId)`

Important boundaries:

- it exists only in manual evidence mode;
- it is not resident cognition;
- it is not gameplay authority;
- it does not auto-trigger by elapsed time;
- it makes adversarial fixture boundaries explicit and inspectable.

For R4:

`scenarioAction("reveal-primary-nearby")`

performs only the external World mutation.

Resident knowledge remains unchanged until the following ordinary local-life step.

This preserves the distinction:

`test stimulus` != `resident reaction`.

---

## 9. Negative evidence / guards respected

### 9.1 Exhaustive execution coordinator guard

Adding `acquire_material_object` caused TypeScript to fail an exhaustive `never` guard in:

`ResidentCausalExecutionCoordinator`

This coordinator owns travel/social execution only.

The fix was **not** to make it secretly execute material intent.

It now explicitly reports material acquisition as unsupported by that coordinator.

Material acquisition remains on its already-earned material competence path.

This preserved subsystem authority rather than expanding one coordinator because the type union grew.

### 9.2 Physical speed clamp

An early R4-C test expected the hidden relocator to arrive exactly at a desired fixture coordinate.

The requested velocity exceeded the actor's actual max speed.

World correctly clamped the motion, producing a different but still genuinely hidden position.

The test was corrected to assert the actual requirement:

> A remains outside legal resident sight.

World physics was not weakened to satisfy an aesthetic test coordinate.

### 9.3 Prior R4-B perception falsification remains valid

Earlier, a supposedly hidden crate was accidentally left within Janek's sight radius.

Janek legally reacquired it and the checked-absence test failed.

That earlier result and the R4-C speed-clamp result establish the same discipline:

> experimental narrative never overrides authoritative geometry/perception.

---

## 10. Architectural result

R4-C required only two meaningful additions:

1. one narrow durable material intent:
   `acquire_material_object`;
2. one resident-relative material reacquisition relevance seam.

It did **not** require:

- GOAP;
- Utility AI;
- behavior-tree expansion;
- a generic affordance graph;
- a new task queue;
- a new scheduler;
- a provider call;
- resident-ID policy;
- broad search autonomy;
- parsing natural-language matter text.

This strengthens the current R4 hypothesis:

> meaningful authored density can emerge from richer composition of World truth + private observation + durable resident meaning + bounded competence before a general affordance framework is justified.

---

## 11. What R4-C does NOT prove

Not proven:

- autonomous selection of broad search areas after checked absence;
- multiple material alternatives can be locally ranked;
- arbitrary material action types share this path;
- ownership/preferences/social norms for material objects;
- dynamic matter creation from every perceived affordance;
- that every visible recognized object deserves relevance;
- that Mira's ordinary low-intensity life benefits from the same substrate;
- that five residents remain healthy under this density;
- that a generic affordance abstraction is now warranted;
- full R4 PASS.

In particular:

> `acquire_material_object` is durable meaning, not permission to turn every object into work.

---

## 12. R4 status after this checkpoint

**R4-A — baseline characterization: COMPLETE**

**R4-B — blocked path + alternate grounded local life: QUALIFIED PASS**

**R4-C — changed World + legal private reacquisition + local reactivation: QUALIFIED PASS**

**R4-D — second-resident / Mira falsifier: OPEN**

**R4 overall: NOT YET PASS**

Next required pressure:

> Can the same causal-affordance substrate improve Mira's ordinary low-intensity life without turning her into a chore bot, making the player central, or generating activity merely because manipulable objects exist?

R4-D must attack the architecture from that opposite direction before R4 can be promoted.
