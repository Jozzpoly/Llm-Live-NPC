# SPC Next — J1 Material Life Slice Checkpoint

Updated: 2026-09-15

This checkpoint records the exact defended state of the first SPC Next material resident-life slice. It is evidence, not a roadmap and not a claim that the living-world milestone is complete.

## Live verdict

> **J0/J1 DETERMINISTIC MATERIAL FOUNDATION DEFENDED · FIRST LOCAL EMBODIED EXECUTOR DEFENDED · PHASER OWNER-OBSERVED SLICE STILL OPEN**

## Defended truth boundaries

### T0c — anonymous speech through Worker

**SCOPED PASS / FULL GREEN**

- anonymous heard speech remains a valid cognition input;
- text, addressed state and bounded directional hearing survive;
- exact hearing coordinates remain illegal;
- `actorId = null` means unrecognized speaker and cannot be reconstructed from guesswork.

Exact qualification: Check #719 PASS.

### T2 — retained semantic evidence reconstructability

**SCOPED PASS / FULL GREEN**

- durable belief/concern state cannot use `CognitionReason.id` as if it were durable World evidence;
- retained semantic state may depend only on resident-acquired percept evidence;
- exact percept projection used by a provider request can remain reconstructable across ordinary recent-memory churn while the retained semantic state still depends on it;
- this remains bounded live provenance, not persistence or an archival memory system.

Exact qualification head: `364374afde15273569596ffd656ac9b59ba75fba`
GitHub Check #727 PASS.

### J0a — World-owned material atom

**MECHANICAL PASS / VISUAL RESEARCH READY**

The first material object is real World state rather than narrative interaction text:

- stable logical object identity;
- `free <-> held` material state;
- World-owned reach and LOS validation;
- one-held-object exclusivity;
- exact factual before/after action results;
- rejected action does not mutate material truth;
- callers cannot spoof actor position or LOS;
- direct participant action exists for the player;
- resident bodies are forbidden from using that direct participant bypass;
- the five-resident specimen contains `crate.workshop.01` and the research renderer has a material representation for it.

Exact qualification head: `42689263abb0bf8f558e2e57474dbe54fb8c8836`
GitHub Check #732 PASS.

**Not promoted:** OWNER-OBSERVED / WORLD-READABLE PASS remains OPEN until the actual browser scene is observed or a genuine browser automation gate exists.

### J1a — exact run authority over atomic material World actions

**SCOPED PASS / FULL GREEN**

Recovered resident manipulation now uses the same resident-private run authority membrane as motion/speech without forcing material actions into a multi-effect frame:

`exact resident run -> atomic material action -> World validation -> factual material outcome`

Defended:

- forged/stale/terminal run cannot mutate material truth;
- World still asks only whether the exact run may mutate World and does not know resident matter semantics;
- pickup/place receive authoritative actor position, reach, LOS and possession checks inside World;
- possession survives the mechanical run that created it because `held` is World state, not a continuously re-issued carry command;
- a later run may act on the already-existing possession fact.

Exact qualification head: `e5096905f981f18fbe7099b8d74ffda4bfd3efe3`
GitHub Check #736 PASS.

### J1b — first recovered local material executor

**SCOPED PASS / FULL GREEN**

The first recovered local-brain executor now performs a bounded embodied competence without LLM frame control:

`bound run -> approach current object position -> stop -> authorized pickup -> factual World result`

It deliberately owns no resident meaning. Meaning/matter remains in `ResidentContinuityKernel`; physical truth remains in `SpcWorldRuntime` / material World state.

Qualification proves:

- movement occurs through the real World motion path;
- the executor uses current World object position rather than a scripted teleport;
- pickup is attempted only through exact run authority;
- factual success can be reconciled back into resident evidence;
- possession persists after run reconciliation;
- suspension/authority loss during approach prevents the old run from continuing into pickup and stale latched motion is revoked.

Exact qualification head: `5958c566d68e3c5a10ce93532cc82ab59a0d3b70`
GitHub Check #738 PASS.

### J1c — Janek composition inside the real five-resident specimen

**DETERMINISTIC COMPOSITION PASS / FULL GREEN**

The actual authored five-resident specimen can now be composed with:

- Janek continuing workshop matter;
- one exact pickup task/run;
- recovered World execution authority;
- the local material pickup executor;
- the authored `crate.workshop.01`;
- factual pickup outcome reconciliation into Janek continuity state.

The qualification runs the composed specimen until Janek physically reaches and picks up the crate, then proves the material World state and resident reconciliation agree.

Exact qualification head: `0a7d289c056cac80d8f34746ab65c3752e53ecd4`
GitHub Check #740 PASS.

## Important nonclaims

This checkpoint does **not** prove:

- Janek has a rich autonomous life;
- interruption/resume is world-readable in the browser;
- attention/facing is authoritative;
- the crate has resident sensory/epistemic representation;
- checked absence/search is restored;
- local obstacle navigation exists;
- a destination/place action is implemented;
- the live Luna/provider loop is connected;
- the research lens exposes the complete evidence -> matter -> run -> World-outcome chain;
- all five residents have recovered life architecture;
- OWNER-OBSERVED or WORLD-READABLE PASS.

## Immediate frontier

1. Safely connect `createFiveResidentJanekMaterialSlice()` to the SPC Next research scene without changing World authority or creating a second simulation clock.
2. Observe the real browser behavior: Janek must visibly approach the same rendered crate and the crate must become held from World truth.
3. Add participant/research observability for Janek's current matter, exact run and factual material outcome without exposing hidden model chain-of-thought.
4. Only then extend the material competence to a real destination/place outcome.
5. Build the first interruption/resume experiment on top of persistent World possession/progress rather than a fake `work` timer.

The intended next promotion is still a vertical living proof, not more horizontal infrastructure:

`Janek has own matter -> embodied material progress -> real interruption -> old matter remains/revises -> resume/abandon -> visible factual continuation`.
