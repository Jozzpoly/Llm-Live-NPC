# SPC R5 — Real Luna Causal Boundary PASS / Semantic Fidelity Finding

Status: **QUALIFIED PASS — REAL-PROVIDER CAUSAL ESCALATION**
Date: **2026-09-21**
Qualified source: `3122baabd64fdf57cc178f29ee98b81b9b8071af`
Parent contract: `docs/SPC_R5_SEMANTIC_ESCALATION_EXPERIMENT.md`

## Promotion claim

R5 qualifies this bounded claim:

> One genuine resident-owned semantic reason can enter real GPT-5.6 Luna cognition exactly once, remain causally inert during asynchronous provider work, return through the same resident-owned admission membrane, materialize only locally grounded authority, execute to factual World consequence, settle its exact source pressure, and remain protected by a local hard request budget that physically prevents a second upstream request.

This closes the post-stress question of whether real provider cognition can be reintroduced as a sparse escalation layer without again becoming the resident's heartbeat.

It does **not** qualify broad semantic/personhood quality.

## Exact live specimen

Scene:
- Mira + Ida only;
- no player;
- no pre-existing Mira matter;
- exact addressed-speech private origin;
- real same-origin `/api/spc-next/life-intent`;
- model: `gpt-5.6-luna`;
- the same resident-generic R5 cognition lane, provider transport, local admission and causal execution used by deterministic R5;
- experiment-owned hard upstream request budget: **1**.

Exact Cloudflare commit preview:

`https://b0f2bbe9-llm-live-npc.jozzpoly.workers.dev`

The preview was resolved from the Cloudflare exact-commit check-run for the qualified SHA, not from a moving deployment alias.

## Verification

### Repository Check

**Check #1532: PASS**

- source: `3122baabd64fdf57cc178f29ee98b81b9b8071af`;
- 252 / 252 test files PASS;
- 940 / 940 tests PASS;
- TypeScript PASS;
- production builds PASS;
- preview verification PASS.

### Browser non-regression

**Browser Evidence #757: PASS**

All existing browser gates R1 through R5-C remained green on the same source.

Chrome:
`Chrome/152.0.7977.82`.

### Live Provider Evidence

**Live Provider Evidence #18: PASS**

The run requested only:
`r5-judgement`.

All older live-provider qualifiers were skipped.

15 / 15 live R5 assertions passed.

## Real provider result

Exact source reason:

`reason:resident.mira:speech:occurrence:1:0`

Private semantic pressure:

> Speech addressed to me: Mira, odpowiesz mi, czy zostaniesz chwilę przy stole?

The provider chose:

- decision: `accept`;
- intent: `communicate`;
- target: `resident.ida`;
- text: `Tak, zostanę chwilę przy stole.`

Provider reason:

> Prośba jest bezpośrednio skierowana do mnie i uzasadnia podjęcie krótkiej rozmowy z rozpoznaną osobą.

Usage:

- model: `gpt-5.6-luna`;
- input: **1,929 tokens**;
- output: **244 tokens**;
- total: **2,173 tokens**;
- provider-reported elapsed: **4,667 ms**;
- browser-observed request elapsed: **4,748 ms**.

This is one observed call, not a latency or token-distribution benchmark.

## Causal result

Provider request began at World tick 2.

The World advanced to tick 122 while the request was outstanding.

At provider completion:

- response was in the inert provider inbox;
- semantic pressure was still `in_flight`;
- no Mira matter existed;
- no run existed;
- provider output had not spoken or moved anything.

At the next explicit World boundary:

- local admission accepted the exact origin;
- one `communicate_actor` matter/run materialized;
- the source semantic reason settled.

The local executor then established physical communication conditions and World authority emitted exactly one factual Mira speech occurrence at tick 199.

The matter resolved from factual delivery evidence:

`succeeded: ... factually delivered speech to resident.ida through occurrence:199:1`.

Provider output itself never emitted the World occurrence.

## Hard-budget safety attack

After the first real judgement had completed, the fixture created a fresh second legal addressed-speech pressure.

The resident loop attempted cognition again.

Hard-budget result:

- `attemptedRequests = 2`;
- `upstreamRequestsStarted = 1`;
- `blockedRequests = 1`;
- `maxUpstreamRequests = 1`;
- `exhausted = true`;
- real provider observations remained exactly **1**.

Therefore this was not merely a counter that happened to remain at one.

The system generated a second demand and the experiment-owned membrane physically stopped it before upstream network inference.

The local rejection created no active ghost matter or World authority.

## Material semantic finding

The live result also exposed an important limit that the green causal assertions do not measure.

The accepted durable intent was:

`communicate_actor`

Its authority and completion contract is only:

> seek/contact a known actor and factually deliver the supplied speech text.

But the text Luna chose was:

> `Tak, zostanę chwilę przy stole.`

That sentence contains a future behavioral/social promise.

The resulting matter resolved when the speech was delivered.

No durable `stay`, `dwell`, `remain-near` or equivalent commitment existed.

Therefore:

> **speech delivery was factual, but the future commitment implied by the speech was not represented by the accepted matter.**

This is not a World-authority breach:
the resident really did say the sentence.

It is a semantic/personhood fidelity gap:
the system can currently let `communicate` assert a future resident obligation that does not survive as causal resident state.

Do not hide this by prompt-tuning the model into avoiding such sentences and then claim the underlying representation problem is solved.

## Judgement-quality classification

The observed Luna answer is conversationally coherent and grounded to a known Ida speech origin.

However broad judgement quality is **NOT QUALIFIED** because:

- one sample proves no distribution;
- current Mira has almost no causal personhood beyond continuity/history;
- `role + drives` remains authored prose, not learned/private preference authority;
- the prompt itself invited a future commitment that current intent vocabulary cannot faithfully represent;
- the resulting speech exposed that representation mismatch.

No extra paid retry is warranted merely to obtain a cleaner answer.

## R5 conclusion

R5 is complete as a **qualified semantic-escalation substrate**.

Earned:
- reason-native provider escalation;
- local life continues during latency;
- inert transport completion;
- exact origin admission;
- stale/newer-attention rejection;
- bounded post-settlement retry;
- explicit decline/defer/clarify/error homeostasis;
- real Luna through the same membrane;
- local hard upstream request cap.

Not earned:
- rich personhood;
- trustworthy future promises/social contracts;
- general semantic judgement quality;
- five-resident provider homeostasis under rich life;
- unattended provider safety beyond explicitly budget-wrapped experiments;
- Owner-observed aliveness.

## Next phase

Return to resident life.

Current recovery frontier:
**R6 — complementary resident falsifiers / ordinary personhood.**

The next experiment should attack the known gap that five authored residents currently have mostly `role + drives` prose plus causal continuity.

It should not add a generic personality vector.

First pressure should require a resident-private, causally originated, persistent self-interest/preference/relationship fact to change ordinary significance or choice before higher cognition explains it.
