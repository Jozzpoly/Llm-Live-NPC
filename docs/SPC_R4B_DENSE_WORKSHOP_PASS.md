# SPC R4-B — Dense Workshop Local-Life PASS

Status: **QUALIFIED PASS — FIRST R4 CAUSAL-AFFORDANCE MILESTONE**
Date: **2026-09-20**
Repository: `Jozzpoly/Llm-Live-NPC`

## 1. Exact qualified source

Code / runtime evidence source:

`5548231763718f868186dd5c07877c65969f7f88`

Immutable source pointer:

`recovery/spc-post-stress-dense-affordance-r4b-qualified-source`

Parent checkpoint:

`recovery/spc-post-stress-causal-personhood-r3-qualified`
at
`27ee3e1d1ca15a528c239d6df23563a2f820a015`

Development line:

`recovery/spc-post-stress-dense-affordance-r4`

Draft PR:

`#146 — recovery: R4 dense authored affordance surface`

This report qualifies the exact source SHA above. Later documentation commits must not be confused with the tested runtime source.

---

## 2. Promotion decision

**R4-B PASS, within a deliberately narrow claim.**

The first dense-affordance milestone is defended:

> A resident can own more than one already-grounded local material matter; when one route becomes truthfully blocked by stale private knowledge, the resident can continue a different still-valid local matter without provider cognition, random task generation or a newly invented priority framework.

A nearby authored material fact can simultaneously remain irrelevant.

After the alternate matter is factually completed, quiet can again be legitimate while the original blocked matter remains unresolved.

This is evidence that a small increase in causal reality can create a richer resident-local life surface through **composition and reuse**, not merely through more activity.

This is **not** full R4 PASS.

R4-C changed-World reacquisition/reactivation and the second-resident falsifier remain open.

---

## 3. Verification

### Repository Check

GitHub Actions:

- workflow: `Check`
- run: **#1461**
- run id: `35525906936`
- source SHA: `5548231763718f868186dd5c07877c65969f7f88`
- result: **SUCCESS**
- test files: **244 / 244 PASS**
- tests: **924 / 924 PASS**
- build gates: **PASS**

### Browser Evidence

GitHub Actions:

- workflow: `Browser Evidence`
- run: **#687**
- run id: `35525906969`
- source SHA: `5548231763718f868186dd5c07877c65969f7f88`
- result: **SUCCESS**
- Chrome: **152.0.7977.82**

All earlier browser vetoes remained green, including:

- normal browser build/evidence analysis;
- manual World control;
- missing-crate epistemic boundary;
- missing-crate embodied recovery;
- player-addressed interruption + exact return;
- Ida social delivery/private-hearing join;
- R1 zero-provider local life.

The new gate also passed:

- **R4 dense-workshop affordance life in real Chromium**.

Therefore R4-B was not promoted from an isolated unit specimen.

---

## 4. Grounded R4-A finding

Executable characterization:

`src/spc-next/r4-affordance-density-baseline.characterization.test.ts`

The ordinary five-resident geographic baseline currently has:

- World bounds `8192 × 8192`;
- 8 regions;
- 7 anchors;
- 1 material object;
- five authored opening activities that all reach `idle` within 900 ticks / 15 seconds;
- no resident-caused material action consequence in that baseline opening/long interval.

This does **not** mean stillness is dead.

R1 already proved that legitimate quiet is a valid living state.

The R4 finding is narrower:

> the large baseline provides weak causal affordance density after its authored opening directives finish.

Long substrate survival is therefore not equivalent to sustained local-life opportunity.

---

## 5. R4-B specimen

Executable composition:

`src/spc-next/r4-dense-workshop-slice.ts`

Deterministic qualification:

`src/spc-next/r4-dense-workshop-slice.integration.test.ts`

Real-browser qualification:

`scripts/spc-browser-r4-dense-workshop-evidence.mjs`

The compact specimen uses three simple material facts.

### A — blocked resident-owned matter

Object:

`crate.r4.remembered`

Matter:

`matter.janek.r4.missing-crate`

Initial run:

`run.janek.r4.pickup-last-known-crate`

Janek legally saw the crate at `(500, 500)` before retreating outside sight while preserving private last-known evidence.

### B — alternate executable resident-owned matter

Object:

`basket.r4.available`

Matter:

`matter.janek.r4.return-basket`

Initial pickup run:

`run.janek.r4.pickup-basket`

Place run:

`run.janek.r4.place-basket`

Destination:

`(1450, 500)`

B already exists as a legitimate continuing matter before A fails.

It is not generated as a reaction to A's failure.

### C — nearby irrelevant authored truth

Object:

`stool.r4.background`

It is real and resident-observable.

No matter references it.

Its mere existence must not manufacture purpose.

---

## 6. Exact causal arc qualified

### Initial

At browser evidence tick 274:

- A is active;
- A's pickup run owns body focus;
- B is active;
- B's pickup run is legally deferred;
- resident-private A knowledge still points to `(500,500)`;
- pending cognition pressure count is 0;
- C exists without creating a matter/run.

### Hidden World change

An external World actor legally relocates A.

Qualified current World truth:

`(1700, 900)`

Janek's private A memory remains:

`(500, 500)`

with the same prior observation tick and `currentlyVisible=false`.

Therefore the relocation does not leak through omniscient object state.

### Checked absence

Janek returns to the remembered position using the already-grounded A run.

Bounded local physical inspection produces:

`checked_absence`

evidence.

The A run reconciles as:

`blocked: material object is not visible after bounded local inspection`

A remains an **active unresolved matter**.

It is not cancelled or silently solved.

R2 pressure receives a real uncertainty reason for the new private discrepancy.

### Automatic alternate-body handoff

Once A's run ends, body focus becomes free.

Exactly one still-authorized deferred run remains: B pickup.

Existing `ResidentExecutionArbitrator` acquires B automatically.

No provider chooses B.

No ranking table says “basket > missing crate”.

No R4-specific opportunity selector exists.

The result follows from a narrower continuity fact:

> there is exactly one already-legitimate unresolved execution demand that can use the free body.

### Factual B consequence

Janek then performs:

- tick 1068 — `material_pickup` B — `picked_up`;
- tick 1304 — `material_place` B — `placed`.

B resolves.

World material truth ends at:

`basket.r4.available -> free at (1450,500)`

A remains active with checked-absence evidence and no active run.

C remains untouched.

### Quiet

From browser tick 1307 to 1907:

- Janek position remains stable;
- A remains unchanged and unresolved;
- B remains resolved;
- C remains unchanged;
- body has no focused/deferred run;
- no new resident material action facts appear.

The system does not invent a new chore merely because a nearby manipulable object exists.

---

## 7. Important negative evidence from the failed first fixture

The first R4-B relocation placed A only around 400 units from its remembered position.

When Janek returned to inspect `(500,500)`, the true crate was still inside his 520-unit sight radius.

The specimen therefore did **not** reach checked absence.

Janek legally reacquired the crate instead.

The first R4-B Check failed.

This was not repaired by:

- shrinking sight range;
- hiding the true object from perception;
- forcing checked-absence evidence;
- changing executor policy.

The fixture was corrected so the external relocation ends genuinely outside the legal reacquisition geometry.

This is useful R4 evidence:

> experimental expectation does not override perception truth.

---

## 8. Architectural result: composition beat framework invention

R4-B did not require:

- a generic affordance graph;
- GOAP;
- Utility AI;
- a chore generator;
- a new opportunity selector;
- a resident-ID policy;
- a new event queue;
- a provider call.

It composed already-earned systems:

- World-owned material truth;
- resident-private material knowledge;
- checked absence;
- continuity matters/runs;
- execution focus;
- deferred-run arbitration;
- local pickup/place competence;
- R2 unresolved pressure lifecycle;
- R1 legitimate quiet.

This is a strong signal for future R4 work:

> grow affordance density first by composing causal truths and resident competences that already exist; generalize only after repeated pressure proves a missing abstraction.

---

## 9. Important boundary: current search is NOT autonomous local recovery

`ResidentMaterialSearchExecutor` is local and epistemically honest once a bounded search plan exists.

However, current donor architecture still treats choosing/grounding the broader search area as semantic/task authority.

Therefore R4-B does not claim:

- autonomous broad missing-object search;
- semantic recovery solved;
- provider no longer needed for all missing-object cases.

The qualified R4-B effect is earlier:

> while one matter is genuinely blocked, another already-grounded local matter can continue without semantic-provider novelty.

That distinction is important.

---

## 10. What R4-B does NOT prove

Not proven:

- changed World truth can later reactivate A through ordinary ongoing private perception;
- quiet residents continuously refresh recognized material observations;
- Janek can locally choose a new broad search plan after checked absence;
- multiple simultaneously executable alternatives can be ranked without higher judgement;
- Mira survives the same affordance architecture without becoming a chore bot;
- dense authored affordances work across all five residents;
- material ownership/relationships/preferences are solved;
- a general affordance abstraction is warranted;
- full R4 is complete.

---

## 11. New open pressure exposed by the PASS

The R4-B end state is intentionally revealing:

- A physically exists elsewhere in the World;
- A remains a meaningful unresolved resident matter;
- Janek is legitimately quiet after B;
- no provider is manufacturing activity.

The next question is now unavoidable:

> **If A later becomes legally observable again, does ordinary resident-local perception refresh private material knowledge and causally reactivate A — or is material perception currently sampled only while a material executor happens to be running?**

That is R4-C.

Do not solve this by making the blocked matter poll hidden World truth.

The reactivation must enter through legal resident observation.

---

## 12. Promotion boundary

**R4-A = characterization complete.**

**R4-B = QUALIFIED PASS.**

**R4 overall = NOT YET PASS.**

Current target:

**R4-C — changed World truth -> legal resident reacquisition -> matter-relative reactivation.**

After R4-C, R4 still requires a second-resident falsifier before full promotion.
