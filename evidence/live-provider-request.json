{
  "version": 22,
  "pr": 148,
  "qualifiers": [
    "r6-endogenous-history-outcome-twin"
  ],
  "purpose": "R6 endogenous ordinary-personhood twin rerun after a material provider-contract repair: structured accept.intent JSON Schema now encodes the same per-kind null/target invariants enforced by the local parser, and the prompt now distinguishes originReasonId causal attribution from recentPercepts-only durable belief/concern evidence. Exact runtime-generated twin contexts are unchanged. Exactly two GPT-5.6 Luna calls maximum and no semantic retry for preferred output. Harness now retains Worker code/diagnostic on rejection.",
  "requestedAtUtc": "2026-09-25T00:17:00Z"
}
