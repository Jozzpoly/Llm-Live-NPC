{
  "version": 3,
  "pr": 125,
  "purpose": "Qualify the exact SPC Next live-provider path after fixing the browser-native fetch receiver bug. Spend-free preflight must first intercept the semantic POST; only then may the workflow call GPT-5.6 Luna.",
  "requestedAtUtc": "2026-09-16T00:34:00Z"
}
